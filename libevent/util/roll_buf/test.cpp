#include "roll_buf.h"
#include <string>
#include <iostream>
using namespace std;
roll_buf<string*> rbuf;

struct work_iterm{
    uint32_t num;
    uint32_t count;
    uint32_t cnt;
    bool stop;
    work_iterm(uint32_t
};

void* produce_work(void*arg){}
